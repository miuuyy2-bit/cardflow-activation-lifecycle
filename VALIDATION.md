# Validation record

Date: 2026-08-16

## Account gate

- The designated CardFlow persistent Chrome session is authenticated to the new GitHub account `miuuyy2-bit`.
- The dashboard reports no repositories and offers “Create your first project.”
- GitHub's exact private commit address was selected for local Git configuration; the primary Gmail address is not stored in this repository.
- No remote repository or Pages site has been created yet.

## Duplicate-intent audit

- Rejected: quote comparison, net-payout calculation and multiplier content because those intents already exist in the Cloudflare Pages notebook and CardFlow workspace.
- Rejected: a general “gift card code not working” troubleshooter because a Google Sites guide already covers that intent.
- Accepted: an issuer-neutral activation lifecycle model focused on state boundaries, responsible systems and evidence limits.

## Local automated checks

Command: `npm.cmd test`

Result: 7 required files, 2 HTML pages, 5 official sources and 5 lifecycle states validated.

`git diff --cached --check` also completed without whitespace errors after staging the local repository.

## Rendered-browser checks

- Desktop: full page rendered with the intended hierarchy and no console warnings or errors.
- Mobile: 390 × 844 viewport; document width remained within the viewport and the table/tab overflow stayed inside its own scroll container.
- Tablet: 768 × 1024 viewport; document width remained within the viewport.
- Interaction: clicking and keyboard arrow navigation both selected the Activation state and updated the tabpanel.
- Accessibility structure: one H1, no blank links, no unlabeled buttons, one active tab stop, and `noopener` on every new-window source link.
- Error route: `/missing-page` returned the custom 404 content with a working relative home link.

## Publication checks still required

1. Create the public repository `miuuyy2-bit/cardflow-activation-lifecycle`.
2. Enable GitHub Actions as the Pages source.
3. Push the local `main` branch and verify the workflow result.
4. Open the public URL independently at desktop and mobile sizes and test the state map again.
